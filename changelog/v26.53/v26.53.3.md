# v26.53.3 (2026-06-19)

## fix: Backend 全面代码审查与安全修复

对 backend 全部 ~591,230 行 Python 代码进行三轮审查（26 个并行 AI agent），发现并修复约 60 项问题。

---

### 🔴 安全漏洞修复（17 项）

| 问题 | 文件 | 修复方案 |
|------|------|----------|
| exec() 远程代码执行 | `workflow/temporal/activities.py` | AST 白名单沙箱 + multiprocessing 子进程隔离（可强制终止死循环） |
| 路径遍历（auto_namer） | `automation/api/auto_namer_api.py` | 添加 `is_relative_to(MEDIA_ROOT)` 校验 |
| 路径遍历（format_api） | `contract_review/api/format_api.py` | 同上 |
| 信息泄露（绝对路径） | `cases/api/casenumber_api.py` | 恢复返回绝对路径（与管理端一致） |
| setattr 批量赋值（10 处） | `cases/contracts/message_hub/contacts/automation` | 全部改为 Pydantic Schema 字段白名单过滤 |
| subprocess 命令注入（2 处） | `cases/admin/views.py`, `contracts/admin/display_mixin.py` | 添加路径白名单校验 |
| 空操作验证器 | `automation/schemas/court_document.py` | 实现真正的字段缺失检查 |
| auth 不一致 | `automation/api/preservation_quote_api.py` | `JWTAuth` → `JWTOrSessionAuth` |

### 🔴 运行时 Bug 修复（15 项）

| 问题 | 文件 | 修复方案 |
|------|------|----------|
| 模板变量永远为空 | `workflow/temporal/workflows.py` | 添加 `context["_last_output"] = result` |
| 条件分支无效 | `workflow/temporal/workflows.py` | 实现 `goto_false` 跳过机制 |
| Wait 步骤无超时 | `workflow/temporal/workflows.py` | 添加 `timeout=timedelta(hours=...)` |
| 信号名不匹配 | `workflow/events/dispatcher.py` | `"court-reply"` → `"gate_approved"` |
| Temporal 地址硬编码 | `workflow/mcp/workflow_tools.py` | 从 Django settings 读取 |
| 未定义方法调用 | `evidence/.../evidence_list_placeholder_service.py` | 实现缺失的两个方法 |
| 错误模块导入 | `evidence/.../evidence_export_service.py` | `evidence.models` → `documents.models` |
| 导入不存在函数 | `litigation_ai/.../prompt_template_service.py` | 移除不存在的导入，返回 None |
| 占位符未格式化 | `legal_solution/services/prompts.py` | 添加 f-string 前缀 |
| 代理模型误用 | `contract_review/admin/format_normalize_admin.py` | `get_or_create(task=task)` → `get(pk=task.pk)` |
| 日志内容为空 | `automation/.../document_processor.py` | 填充 `file_names` 列表 |
| 利率静默替换 | `finance/services/lpr/sync_service.py` | 添加 `logger.warning()` |
| except 过宽 | `workflow/temporal/workflows.py` | `except Exception` → `except asyncio.TimeoutError` |
| 超时后状态未清理 | `workflow/temporal/workflows.py` | 添加 `_current_gate_step_id = None` |
| 跳过目标缺失无告警 | `workflow/temporal/workflows.py` | 添加 `logger.warning` |

### 🟡 规范与结构修复（10 项）

| 修复 | 文件 | 说明 |
|------|------|------|
| 删除空壳 app | `apps/fee_notice/` | 目录内无 .py 文件 |
| 移动 ML 模型 | `apps/models/` → `assets/ml_models/` | ONNX 模型不应放在 apps/ 下 |
| 目录重命名 | `test_tools_hub/` → `tools_hub_admin/` | 避免与测试目录混淆 |
| 信号迁移 | `batch_printing/signals.py`, `pdf_splitting/signals.py` | 从 models.py 迁移到独立 signals.py |
| 核心模型时间戳 | `cases/models/case.py`, `contracts/models/contract.py`, `client/models/client.py` | 添加 created_at/updated_at + migration |
| 共享常量 | `core/constants.py` | 文件大小、日期格式常量集中管理 |
| 魔法数字替换 | 6 个文件 | HTTP 状态码、文件大小常量统一 |
| 空目录清理 | `litigation_ai/admin/`, `document_parsing/tests/` | 删除仅含空 `__init__.py` 的目录 |
| 审查报告 | `AUDIT_REPORT.md` | 完整审查报告（安全/性能/架构/类型等 13 个维度） |

### Migration 文件

| Migration | 说明 |
|-----------|------|
| `cases/migrations/0028_add_timestamps_to_case.py` | Case 添加 created_at/updated_at（两步安全方案） |
| `contracts/migrations/0034_add_timestamps_to_contract.py` | Contract 添加 created_at/updated_at |
| `client/migrations/0010_add_timestamps_to_client.py` | Client 添加 created_at/updated_at |

### 新增文件

| 文件 | 说明 |
|------|------|
| `backend/AUDIT_REPORT.md` | 完整代码审查报告（13 个维度） |
| `backend/apps/core/constants.py` | 项目共享常量（文件大小、日期格式、HTTP 超时） |
| `backend/apps/batch_printing/signals.py` | batch_printing 信号处理 |
| `backend/apps/pdf_splitting/signals.py` | pdf_splitting 信号处理 |

### 审查覆盖维度

安全漏洞（OWASP Top 10）、运行时 Bug、God Class、N+1 查询、异常处理、依赖耦合、数据库性能、模型设计、API 设计一致性、异步任务系统、类型安全、文件命名规范、文件夹结构
