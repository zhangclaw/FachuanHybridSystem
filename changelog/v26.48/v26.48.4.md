## [26.48.4] - 2026-05-11

### 前端

#### 修复

- **批量分析结果卡片 JSON 解析**：`parseBatchResult` 解析前先剥离 ` ```json ` 代码围栏，数据库中已有的带围栏 JSON 内容能正确渲染为结构化卡片，不再回退到原始代码块
- **助手消息裸露 JSON 自动包裹**：`MarkdownContent` 预处理新增 `wrapBareJson` 检测，LLM 输出的未包裹 JSON 自动识别并包裹为 ` ```json ` 代码块，支持语法高亮和复制按钮
- **用户消息气泡宽度适配**：消息组和气泡添加 `w-fit`，短消息不再撑满 `max-w-[75%]`，宽度随内容自适应
- **用户消息换行显示**：修复 `\n` 被渲染为可见换行的问题
- **统一按钮样式**：「继续添加」和「开始转换」按钮样式一致

#### 新功能

- **DOC 转 DOCX 工作台支持拖拽文件夹**：可直接拖入文件夹批量转换
- **用户可配置 LLM 评分并发数**：法律研究任务支持自定义并发数，397B 模型自动设为 100

### 后端

#### 新功能

- **OpenAI 兼容 LLM 后端**：新增通用 OpenAI 兼容接口后端
- **LLM 评分并发数配置**：任务模型新增 `llm_scoring_concurrency` 字段，支持按任务调整并发

#### 修复

- **批量分析取消状态**：取消后任务状态正确更新为 `cancelled`
- **模型切换**：默认模型从 Qwen3.5-397B-A17B 切换为 kimi26，关闭思考模式
