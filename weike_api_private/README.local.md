# Local-only API Research Implementation

This directory is intentionally git-ignored.
Put private API login/search implementations here if needed.
The shipped runtime currently uses Playwright-only search flow.
