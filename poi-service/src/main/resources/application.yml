server:
  port: 8090

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

poi:
  service:
    # Template directory for pre-designed DOCX templates
    template-dir: classpath:templates
    # Output directory for generated documents
    output-dir: /tmp/poi-output
